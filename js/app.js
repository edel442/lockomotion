'use strict';

// alle Eventhandler initialisieren ("der Mannschaft ihre Aufgaben geben")
// wird unten in $(document).ready() === die init-function als Parameter
// übergeben

var init = function() {
	$('.carousel').carousel();
};
